# Backend (FastAPI) base URL. Run: uvicorn api:app --reload
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Mapbox: create a free token at https://account.mapbox.com/
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_token_here
